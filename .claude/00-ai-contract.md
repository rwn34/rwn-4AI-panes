# AI Contract (pointer)

Claude Code's actual loaded AI contract is at **`/CLAUDE.md`** (project root) — that's
Claude Code's native always-loaded memory path. Not inside this folder, despite the
naming symmetry with `.kimi/steering/00-ai-contract.md` and
`.kiro/steering/00-ai-contract.md`.

This file exists in `.claude/` for cross-CLI discoverability: any CLI (Kimi, Kiro) or
human browsing `.claude/` expecting to find Claude's contract here gets a breadcrumb
instead of confusion.

## To edit Claude's contract
Edit **`/CLAUDE.md`** at project root. Do not edit this file — it is documentation only.

## Claude Code's native paths (for other CLIs touching Claude's setup)

| Purpose | Path | Auto-loaded? |
|---|---|---|
| AI contract / always-loaded memory | `/CLAUDE.md` | yes |
| On-demand skills | `.claude/skills/<name>/SKILL.md` | triggered by `description` frontmatter |
| Settings / hooks / permissions | `.claude/settings.json`, `.claude/settings.local.json` | yes |
| This breadcrumb | `.claude/00-ai-contract.md` | no (pointer only) |

## Full multi-CLI layout
See `.ai/README.md`.
